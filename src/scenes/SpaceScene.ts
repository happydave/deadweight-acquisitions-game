import Phaser from 'phaser'

export class SpaceScene extends Phaser.Scene {
  constructor() {
    super({ key: 'SpaceScene' })
  }

  create(): void {
    // Empty — entities introduced in later work items
  }
}
